### Added

- Add the Heze fork scaffolding: `HEZE_FORK_VERSION`/`HEZE_FORK_EPOCH` and the EIP-7805 config values, the Heze preset constants in `config/fieldparams`, `version.Heze` (gated as unsupported), the fork-schedule entry, `ToForkVersion` and `CanUpgradeToHeze`. No Heze consensus types and no EIP-7805 logic yet.
