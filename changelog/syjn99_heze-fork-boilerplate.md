### Added

- Add the Heze fork boilerplate: `HEZE_FORK_VERSION`/`HEZE_FORK_EPOCH` and the EIP-7805 config values, `version.Heze` (gated as unsupported), `UpgradeToHeze`, the Heze block/state/bid protobuf types and the p2p, DB, API and validator client wiring. No EIP-7805 logic; post-Heze behaves exactly like Gloas.
